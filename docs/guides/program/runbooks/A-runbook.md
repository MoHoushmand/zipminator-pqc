# Human-action runbook — Track A
_Generated by stage-human-actions.sh. Placeholders only; no live secrets. You execute these._

## Waitlist pipeline (Supabase + Resend)
1. Run migration on prod Supabase: apply `web/supabase/migrations/0001_waitlist.sql`.
2. Mint NEW Supabase keys (project uogbyl…); set in Vercel + local `web/.env.local`:
   - `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` (or publishable), `SUPABASE_SERVICE_ROLE_KEY`
3. Create a Resend API key, set `RESEND_API_KEY` in Vercel; verify `qdaria.com` SPF/DKIM in Resend.
4. `vercel env add` for each (production + preview), then redeploy.
5. Verify: `curl https://www.zipminator.zip/api/waitlist` → `supabase: configured`; submit the form → 200 + row + email.
