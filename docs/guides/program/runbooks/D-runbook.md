# Human-action runbook — Track D
_Generated by stage-human-actions.sh. Placeholders only; no live secrets. You execute these._

## PyPI publish (OIDC trusted publishing — no token)
1. After T0 reconcile lands on main: `git tag -a v0.5.1 -m "zipminator 0.5.1" && git push origin v0.5.1`.
2. Approve the `pypi` GitHub Environment deployment if required reviewers are set.
3. Verify: `pip install zipminator==0.5.1`.
