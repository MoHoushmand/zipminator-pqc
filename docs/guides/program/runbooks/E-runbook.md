# Human-action runbook — Track E
_Generated by stage-human-actions.sh. Placeholders only; no live secrets. You execute these._

## iOS TestFlight secrets (code already parameterised)
1. Create private certs repo `QDaria/certificates` (empty, private).
2. `cd app/ios && bundle exec fastlane match init` (seed appstore cert/profile for com.qdaria.zipminator).
3. Register GitHub Actions secrets (names only): `ASC_API_KEY_ID`, `ASC_API_KEY_ISSUER_ID`, `ASC_API_KEY_CONTENT`, `MATCH_GIT_URL`, `MATCH_PASSWORD`, `FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD`, plus env `ITC_TEAM_ID`.
4. Tag a build on reconciled main to trigger `.github/workflows/testflight.yml`.
