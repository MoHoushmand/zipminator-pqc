# Human-action runbook — Track I
_Generated by stage-human-actions.sh. Placeholders only; no live secrets. You execute these._

## Android keystore + Play upload
1. Generate upload keystore; populate `app/android/key.properties` from `key.properties.example` (never commit).
2. `flutter build appbundle --release`; `bundletool` dry-run validate.
3. Upload AAB to Play Console internal testing.
