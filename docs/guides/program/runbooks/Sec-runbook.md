# Human-action runbook — Track Sec
_Generated by stage-human-actions.sh. Placeholders only; no live secrets. You execute these._

## Secret rotation (USER ONLY)
- Rotate Supabase anon+service keys; rotate Resend key; DELETE dead `SENDGRID_API_KEY`.
- Regenerate OAuth secrets (GitHub/Google/LinkedIn) + `AUTH_SECRET` (`openssl rand -base64 32`).
- Rotate root cloud/quantum keys (Anthropic/AWS/IonQ/QBraid/Gemini/OpenRouter/Flask/entropy).
- Replace `web/.env -> ../.env` symlink with a web-scoped gitignored `.env.local`.
- Review + merge consolidated dependabot PRs.
